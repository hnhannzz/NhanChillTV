# Runtime database

This directory stores JSON runtime data on the server.

The real files are ignored by git:

- `data.json`
- `users.json`
- `comments.json`

The backend creates missing files automatically. Example files are included only as templates for a clean GitHub repository.
